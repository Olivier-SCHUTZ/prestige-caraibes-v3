# =========================================================
# .gitignore pour Prestige Caraïbes (Racine: /wp-content/)
# =========================================================

# --- SYSTÈME & IDE ---
.DS_Store
.cache
.vscode/
.idea/
*.log
*.swp

# --- DOSSIERS WP STANDARDS (À IGNORER) ---
uploads/
upgrade/
cache/
languages/
updraft/
ai1wm-backups/
wflogs/
*-cache/

# --- PLUGINS ---
# 1. Ignorer tous les plugins
plugins/*

# 2. Autoriser le dossier parent pour que les exceptions fonctionnent
!plugins/

# 3. Autoriser tes plugins maison
!plugins/pc-*/

# 4. Ignorer les dépendances lourdes dans tes plugins
plugins/pc-reservation-core/vendor/

# --- MU-PLUGINS ---
# On s'assure que tout ce qui est dans mu-plugins remonte
!mu-plugins/

# --- WP ROCKET ---
# On ignore tout le dossier de config (tes fichiers .json et .html de la capture)
wp-rocket-config/

# --- COMPOSER (Nettoyage) ---
# On ignore les fichiers d'installation de composer
plugins/pc-reservation-core/composer-setup.php
plugins/pc-reservation-core/composer.phar

# --- THEMES ---
# Par défaut, tout est suivi dans themes/. 
# Si tu veux n'en suivre qu'un seul, on peut restreindre, 
# mais sinon, Git suivra tout le dossier themes/ par défaut.