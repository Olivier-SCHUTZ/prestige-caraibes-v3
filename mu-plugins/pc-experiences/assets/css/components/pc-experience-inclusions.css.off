/* ============================================================
   Composant : Experience Inclusions
   Shortcode : [experience_inclusions]
   ============================================================ */

.exp-inclusions-section {
  padding: 2rem 0;
}

.exp-inclusions-grid {
  display: grid;
  gap: 2.5rem;
  /* Desktop : 3 colonnes */
  grid-template-columns: repeat(3, 1fr);
}

/* Tablette : 2 colonnes */
@media (max-width: 1024px) {
  .exp-inclusions-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Mobile : 1 colonne */
@media (max-width: 767px) {
  .exp-inclusions-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
}

.exp-inclusions-col .exp-inclusions-title {
  display: flex;
  align-items: center;
  gap: 0.75rem; /* Espace entre l'icône et le texte */
  font-family: var(--pc-font-title);
  font-size: 1.2rem;
  font-weight: 600;
  color: var(--pc-color-heading);
  margin-top: 0;
  margin-bottom: 1rem;
}

.exp-inclusions-col .exp-inclusions-title i {
  color: var(--pc-color-primary);
  font-size: 1.1em; /* Icône légèrement plus grande que le texte */
}

.exp-inclusions-content {
  font-family: var(--pc-font-body);
  font-size: 1rem;
  line-height: 1.7;
}

.exp-inclusions-content p {
  margin: 0 0 0.5em 0;
}
.exp-inclusions-content p:last-child {
  margin-bottom: 0;
}

.exp-inclusions-content ul {
  list-style: none;
  padding-left: 0;
  margin: 0;
}

.exp-inclusions-content li {
  padding-left: 1.75em;
  position: relative;
  margin-bottom: 0.5em;
}

.exp-inclusions-content li::before {
  content: "✓";
  position: absolute;
  left: 0;
  top: 0;
  color: var(--pc-color-primary);
  font-weight: bold;
  font-size: 1.2em;
}
